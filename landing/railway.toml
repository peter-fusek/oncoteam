[deploy]
# Landing is a static nginx image. We ship a `/health` file alongside the HTML
# so Railway's healthcheck gets a deterministic 200 text/plain response.
overlapSeconds = 15
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
